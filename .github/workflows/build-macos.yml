name: Build macOS (placeholder)

# Milestone 11 target. Workflow kept so the CI layout matches the product plan.
# Jobs are skipped until a macOS agent package exists.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: macos-latest
    steps:
      - run: echo "macOS builds are not implemented yet (Milestone 11)."
